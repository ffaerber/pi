# CPU idle
100 - (avg by (instance) (irate(node_cpu{job="node_exporter",mode="idle"}[5m])) * 100)

# process_cpu_seconds_total
rate(process_cpu_seconds_total[15s]) * 100

sum (rate (process_cpu_seconds_total{id="/"}[1m])) / sum (machine_cpu_cores) * 100


all CPUs
sum(count(count(node_cpu) without (mode)) without (cpu))
